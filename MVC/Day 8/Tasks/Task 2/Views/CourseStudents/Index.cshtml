@model Task_2.Models.Course

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Edit" asp-route-id="@Model.Id">Edit</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                Topic
            </th>
            <th>
                CourseGrade
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Students)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Email)
                </td>
            </tr>
        }
    </tbody>
</table>
